# graph-search-algorithms 项目状态记录 v0.1

## 1. 项目名称

graph-search-algorithms

## 2. 当前阶段状态

- 当前阶段：MVP v0.1 冻结状态
- 当前主分支：master
- MVP v0.1 已完成
- 当前工作区应为 clean

## 3. 已完成内容

- 建立了基础无向图数据结构 `Graph`
- 实现了广度优先搜索 `bfs`
- 实现了深度优先搜索 `dfs`
- 添加了最小 pytest 测试
- 添加了最小命令行演示入口
- 添加了 pytest 基础配置
- 更新了项目 README，记录运行和测试方式

## 4. 已验证命令

```bash
python3 main.py
pytest
```

验证结果：

- `python3 main.py` 可以输出 BFS 和 DFS 示例结果
- `pytest` 通过全部 MVP v0.1 测试

## 5. Git / GitHub 状态

- 当前主分支：master
- PR #1 已合并
- tag 已创建：v0.1-mvp
- 本地 `codex-graph` 分支已删除
- 远程 `codex-graph` 分支已删除
- 当前工作区应为 clean

## 6. 当前冻结点

MVP v0.1 冻结在基础图结构、BFS、DFS 和最小测试全部完成后的状态。

该冻结点适合作为后续扩展 A* 搜索、图可视化、更多测试用例或项目结构完善的稳定起点。

## 7. 后续任务候选

- 实现 A* 搜索算法
- 增加加权图支持
- 增加有向图支持
- 为 BFS / DFS 增加路径返回能力
- 添加更多边界测试，例如孤立节点、重复边、空图
- 增加简单图可视化功能
- 清理历史中已被 Git 跟踪的 `.DS_Store`

## 8. 下次恢复项目的建议起点

建议下次从 A* 搜索算法的设计计划开始，而不是直接写代码。

推荐恢复顺序：

1. 确认当前分支是 `master`
2. 确认工作区 clean
3. 查看 tag `v0.1-mvp`
4. 阅读当前状态文档
5. 为 A* 搜索单独制定 v0.2 开发计划

## 9. 暂停说明

项目已在 MVP v0.1 阶段暂停。

暂停时不需要继续修改代码、不需要提交新的 commit、不需要推送远程分支。后续恢复时，应先基于当前冻结状态确认目标，再开启新的计划和实现步骤。
